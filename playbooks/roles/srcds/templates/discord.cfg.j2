"Discord"
{
    "crash"
    {
        "url"   "{{ discord.crash_webhook }}"
    }
    "stac"
    {
        "url"   "{{ discord.stac_webhook }}"
    }
    "calladmin"
    {
        "url"   "{{ discord.calladmin_webhook }}"
    }
    "sourcebans"
    {
        "url"   "{{ discord.stac_webhook }}/slack"
    }
    "sourcecomms"
    {
        "url"   "{{ discord.stac_webhook }}/slack"
    }
    "test_discord"
    {
        "url"   "{{ discord.stac_webhook }}"
    }
    "test_slack"
    {
        "url"   "{{ discord.stac_webhook }}/slack"
    }
}